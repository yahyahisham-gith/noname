-- Direct-Stream Version Loader
-- Bypasses local and executor cache to pull EXACTLY what's on GitHub

local github_url = "https://raw.githubusercontent.com/yahyahisham-gith/noname/main/admin-hub-V1"
local cache_buster = "?v=" .. tick() -- Forces fresh download

local code = game:HttpGet(github_url .. cache_buster)

if code then
    print("Admin Hub: Pulled latest commit successfully.")
    loadstring(code)()
else
    warn("Admin Hub: Failed to pull from GitHub.")
end
